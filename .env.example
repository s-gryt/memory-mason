# Memory Mason — Environment Configuration
# Copy to .env in your project root to configure vault access

# Required if not using memory-mason.json or global config
MEMORY_MASON_VAULT_PATH=

# Optional: override the subfolder (default: ai-knowledge)
MEMORY_MASON_SUBFOLDER=
